{
  "name": "claude-sandbox",
  "version": "1.0.0",
  "description": "Create isolated Docker sandboxes with Claude Code",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "claude-sandbox": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.7",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.2",
    "dockerode": "^4.0.9",
    "fs-extra": "^11.3.3",
    "inquirer": "^13.1.0",
    "zod": "^4.3.4"
  }
}
